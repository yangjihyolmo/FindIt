# FindIt

Database Configuration

1. Create a PostgreSQL database named:
   lostfound_db

2. Import:
   database/schema.sql

3. Open:
   config/db.php

4. Update:

$username = "your_postgresql_username";
$password = "your_postgresql_password";

5. Run the project using XAMPP/Apache.